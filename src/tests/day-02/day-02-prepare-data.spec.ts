import { prepareData } from "../../day-02/prepare-data-02";

describe('data parsing', () => {
    it('should convert text file to object', () => {
        // var result = navigationParameter[];
        // expect(prepareData("src/data/test.txt", "utf-8")).toBe(result)
    });
});